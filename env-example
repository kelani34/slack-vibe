# Supabase Configuration
# Retrieve these from your Supabase Project Settings > Database
DATABASE_URL="postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.[ref]:[password]@aws-0-[region].supabase.co:5432/postgres"

# Supabase API
NEXT_PUBLIC_SUPABASE_URL="https://[ref].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="[anon_key]"

# Next Auth
# Generate using `openssl rand -base64 32`
AUTH_SECRET="super-secret-key-change-me"
AUTH_URL="http://localhost:3000"

# OAuth Providers (Optional for local dev, needed for flow)
AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""
