# Decisions, assumptions and open questions

[Index](README.md) · [Product](product.md) · [Timeline](timeline.md)

## Confirmed instructions

| ID | Decision | Consequence |
|---|---|---|
| D01 | Original assessment round was assessment/documentation only | Historical constraint; the later authorized implementation follow-up may change app code, while docs remain the scope authority |
| D02 | Documentation must be detailed and connected | Evidence → features → designs → work packages → timeline → tests |
| D03 | Implementation needs to fit a day | Eight-hour restricted candidate with explicit gates/cuts |
| D04 | The product should use polished animation broadly across feature surfaces, inspired by Emil Kowalski's precise interaction work | Shared motion tokens and feature-level transitions; immediate input, stable reading, reduced-motion alternatives and performance limits remain mandatory |
| D05 | Assess speed with a 10× ambition | Establish measured baselines and targeted work reduction, never invent timings |
| D06 | Assess architecture and what should move | Incremental responsibility changes, not an immediate rewrite |
| D17 | Expand routes and features to cover a production application | Add the R01–R52 design and F49–F68 requirements, with independent priorities and incremental effort; documentation only |
| D18 | Define at least 30 concrete application features and expand product scope | Publish an initial numbered 60-feature catalogue and specify ten new collaboration capabilities F69–F78; distinguish current coverage, proposed production work and expansion |
| D19 | Include voice/video, scheduling and production-grade Slack/Discord breadth | Calls and scheduled meetings become explicit full-target requirements; add F79–F108, R59–R78 and W31–W39; earlier exclusions are superseded, while current implementation remains dependency-ordered |
| D20 | The app must be highly mobile responsive | Apply F40 and MR01–MR14 to every enabled route; phone/tablet, keyboard/touch, media/admin and physical-device checks are release requirements, separate from dedicated native clients |
| D23 | The whole application must use TDD with tests throughout | Red/green/refactor for every behavior/fix, characterization before refactors, all-layer tests, F01–F108 traceability, coverage/CI gates and test-foundation-first sequencing |
| D24 | Add a QA engineer to check every feature and verify expected behavior without regressions | Dedicated independent QA ownership, 90-feature/78-route execution ledger, reproducible defects, fix retests and candidate-specific release sign-off; role documented, no execution claimed |
| D25 | Provide a full dependency-ordered implementation/timeline plan for all features and inspect with the browser | Initial plan snapshot: P00–P14, 90/108/78 traceability and 642 planned feature cases; later additions bring the catalogue to 649. Explicit repair/QA gates and separate QA allowance; browser inspection identifies auth blockers; selected implementation slices do not relabel the full catalogue complete |

## Proposed technical decisions

These are recommendations for a future implementation session, not changes already made.

| ID | Proposal | Rationale | Revisit trigger |
|---|---|---|---|
| D07 | Retain Next.js/Prisma/PostgreSQL/Supabase | Existing substantial feature investment; root issues are boundaries and correctness | Measured provider/runtime limitation |
| D08 | Query owns server data; Zustand owns transient view state | Reduce conflicting cache authorities | A concrete state requirement not served by Query |
| D09 | Shared access policy precedes performance/cache expansion | Avoid making unauthorized reads faster or longer-lived | Never waive, only refine policy |
| D10 | One realtime reconciliation owner | Eliminate duplicate events/fetches and inconsistent view updates | Independent transport scopes justify separate bounded consumers |
| D11 | Use CSS/Radix/Sonner for straightforward feedback and prototype a scoped motion runtime for complex sequences | The user wants detailed motion throughout the product; a small expressive design system should serve common transitions, and complex coordinated scenes must be evaluated against bundle, lifecycle and performance costs | Choose a runtime after representative prototypes and profiling |
| D12 | Explicit publication status/time and one scheduler | Prevent early scheduled disclosure and duplicate publication | Migration analysis favors a separate draft table |
| D13 | Private object storage and signed grants | File audience must follow channel audience | Explicit public-asset product feature |
| D14 | PostgreSQL search/index improvements first | Existing DB is sufficient starting point and avoids another service | Measured relevance/latency/scale failure |
| D15 | One-day feature cuts require backend/route/transport gating | Hidden controls cannot close access gaps | Feature acceptance tests pass |
| D16 | Polling is an explicit internal fallback if realtime identity fails | Keep reads authorized without loosening policies | Secure realtime integration passes |
| D21 | Canonical `/w/[workspaceSlug]` workspace namespace | Avoid global-page/slug collisions and distinguish channel/DM destinations | Compatibility audit; migration excluded from day-one critical path |
| D22 | Primary workday hubs with secondary destinations under More | Grow capability without overwhelming daily navigation; mobile list screens use the five-entry contract and focused details use contextual controls | Usability evidence, not route count |

## Assumptions and unresolved product choices

| ID | Unknown | Working assumption | Decision deadline / owner |
|---|---|---|---|
| O01 | Team capacity and actual implementation start | One developer plus independent QA assumed for full plan; eight-hour restricted candidate; H0/T0 relative | Before implementation, project owner |
| O02 | First-release audience and launch mode | Small invited/internal team first | Before any external release, project owner |
| O03 | Expected workspace/message volume | Synthetic small/medium fixtures in testing doc | Before performance sign-off |
| O04 | Deployment provider and app/DB regions | GitHub About links to Vercel deployment; retain if viable, regions/runtime configuration unverified | W19 start |
| O05 | Live RLS, bucket, identity and cron setup | Unknown; source scripts do not prove deployment | W03 and day-one trust gate |
| O06 | Public-channel read-before-join policy | Discoverable in workspace; full history requires membership | W02, product owner |
| O07 | Admin access to private conversations | No implicit content access; membership changes explicit/audited | W02/W15 |
| O08 | Reply posting restrictions | Same permission as channel posting | W02 before changing existing exception |
| O09 | Pinning and edit policy | Channel members may pin; retain 30-minute author edit window from publication | W10 |
| O10 | Retention, deletion, export and compliance needs | Tombstones proposed; retention duration not chosen | Before public release/deletion feature |
| O11 | File types, limits and scanning | Small private upload allowlist; limits in security doc are proposals | W09 before enabling upload |
| O12 | Brand accent and theme preference | Retain current font/tokens for day one; propose refined dual themes later | W16 visual review |
| O13 | Presence and hidden-user scope | Presence ephemeral; hiding is display preference; scope needs decision | W13 |
| O14 | Invite link policy | Restrict invitation material to permitted roles; rotation needed | W02/W15 |
| O15 | Public/restricted release expectation at end of day | Delivery candidate; no external launch if gates fail | H8 decision |

These open items do not block creating a useful specification. They must not be silently treated as user-approved implementation behavior where they materially affect privacy, data retention, scope or external release.

## Source references and limits

Repository evidence is primary for current behavior. External sources inform narrow platform/motion recommendations; exact installed-version behavior still requires implementation verification. Reviewed 18 September 2026.

| Source | Used for |
|---|---|
| [Next.js data security](https://nextjs.org/docs/app/guides/data-security) | Server Action boundary and server-side access responsibility |
| [Next.js caching without Cache Components](https://nextjs.org/docs/app/guides/caching-without-cache-components) | Matching cache guidance to the app's current configuration |
| [TanStack Query advanced SSR](https://tanstack.com/query/latest/docs/framework/react/guides/advanced-ssr) | Request-scoped prefetch/dehydrate approach |
| [Supabase Postgres Changes](https://supabase.com/docs/guides/realtime/postgres-changes) | Subscription/RLS/publication verification |
| [Supabase Realtime authorization](https://supabase.com/docs/guides/realtime/authorization) | Private Broadcast/Presence topic authorization |
| [Supabase third-party auth](https://supabase.com/docs/guides/auth/third-party/overview) | Identity integration investigation, not an assertion of automatic NextAuth compatibility |
| [Supabase Storage access control](https://supabase.com/docs/guides/storage/security/access-control) | Private access and service-role trust boundary |
| [PostgreSQL text-search indexes](https://www.postgresql.org/docs/current/textsearch-indexes.html) | GIN as a full-text index candidate |
| [Emil Kowalski: You Don't Need Animations](https://emilkowal.ski/ui/you-dont-need-animations) | Frequency-aware and purpose-driven restraint |
| [Emil Kowalski: Good vs Great Animations](https://emilkowal.ski/ui/good-vs-great-animations) | Spatial origins and easing precision |
| [GSAP React guidance](https://gsap.com/resources/React/) | Scoped lifecycle if complex JS motion is later justified |
| [WCAG 2.2 quick reference](https://www.w3.org/WAI/WCAG22/quickref/) | Accessibility criteria and exceptions for implementation validation |
| [Web Vitals](https://web.dev/articles/vitals) | LCP, INP and CLS thresholds; project budgets remain separately proposed |

Skills applied: Ponytail (full), TypeScript Anti-Slop, Impeccable product guidance, Frontend Design as supporting visual guidance, and GSAP core/React/performance guidance for motion tradeoffs. The motion direction follows the user's request for broad, high-detail animation, with interaction clarity, reduced-motion support and measured performance as quality requirements. No new library is installed by this documentation.

## Change log

- 2026-09-25 D04 state-language slice: Profile and thread initial loading now announce a named status and reserve the shape of arriving content. Shared skeleton pulse is disabled under reduced motion. The 243-test full-suite passes; independent QA and other route states remain open, including member, empty, error, denied, archived, revoked and action-progress states.
- 2026-09-25 D03 adaptive-overlay completion: Added source contracts and dynamic viewport bounds to shared Dialog/Popover primitives, retained reachable overflow, and replaced file preview's static `90vh` with 1rem `dvh`/`vw` margins. All 241 local tests pass; B19 measures a 288×192 popover and 288×536 PDF preview inside the exact 320×568 production viewport. Zoom, software keyboard, focus return/Escape nesting, physical-device and independent QA remain open.
- 2026-09-25 D02 icon-control completion: Added a source-wide AST contract for accessible app icon Buttons and native icon-only buttons, 44px compact minimums for shared icon Button/Toggle variants, explicit composer/thread/preview/profile/activity/schedule/star labels, keyboard-visible attachment/notification actions and non-submitting removal controls. All 238 local tests pass; exact production evidence B18 covers the composer/formatting targets at 390×844, while keyboard, assistive-technology, physical-device and independent QA remain open.
- 2026-09-25 D01 semantic-token completion: Added measured unread, success, warning, favorite and message-target roles; migrated notification icons, unread badges/dividers, presence, schedules, stars, channel warning/about copy, profile links and message targeting. The contract now rejects raw app-owned palette classes and hex values across source while retaining only Recharts' third-party SVG attribute selectors as an explicit adapter exception. All 235 local tests pass; light visual, forced-colors and independent QA remain open.
- 2026-09-25 D01 semantic-token checkpoint: Added light/dark `saved` and `pinned` foreground/surface roles, migrated message bookmark/pin state, the pinned/bookmarked panel and editor mentions, and added a contract that both rejects raw state colors in those consumers and calculates contrast from the declared OKLCH values. Current ratios exceed 7:1 in every checked pair. The application scan still finds 36 raw palette/hex usages; notification, unread, presence, schedule, destructive and highlight roles remain open.
- 2026-09-25 CI runtime checkpoint: GitHub Actions now uses `actions/checkout@v7` and `actions/setup-node@v7` while the application runtime remains pinned to Node 22.23.2. Hosted run [36126641556](https://github.com/kelani34/slack-vibe/actions/runs/36126641556) passed documentation, unit, component, integration, type, build and lint gates on commit `6cfdf55`; the prior Node 20 action-runtime deprecation is gone. GitHub's Ubuntu 26 migration notice remains an informational runner-image item.
- 2026-09-25 localhost schema checkpoint: The exact production artifact exposed `P2022` because the active local datasource had three of eight committed migrations. Applied the five pending additive migrations atomically with their Prisma-compatible checksums through the working runtime pooler after the configured direct connection stalled before SQL. Verified all eight history rows, the idempotency columns, Saved Items and a populated channel. `DIRECT_URL` reachability and separate staging/production classification remain operational gates.

- 2026-09-24 implementation/design follow-up: Corrected the motion direction to match the user's explicit request for high-detail animation across the product. Expanded M01–M30, added responsive/reduced-motion contracts for DMs, calls and collaboration features, added a reduced-motion-safe group-avatar entrance, and made group avatar stacks an explicit F14 acceptance/QA case. Raised W17 to 4–7d, P02 QA to 4–7d and recomputed the full forecast to 165–309 development/shared-test days plus 65–124 QA days. The local candidate then had 58 passing tests, clean typecheck/build/lint, and browser evidence for a two-avatar group DM plus peer-aware one-to-one composer text; hosted CI and independent QA remained pending.
- 2026-09-24 auth foundation follow-up: Migrated the Next.js 16 deprecated `middleware.ts` convention to `src/proxy.ts`. A test-first auth boundary suite covers anonymous protected-route redirects, public invitations, authenticated protected routes, login redirects and matcher coverage; it exposed and fixed the matcher excluding `/login`, which had made the signed-in redirect unreachable. Protected redirects preserve path/query, and both the login action and signed-in login redirect validate a local return path. Host-like, repeated, protocol-relative and backslash-normalized destinations fall back to `/`. The account-menu Log out control invokes Auth.js client sign-out and returns to `/login`, and known/unknown provider failures render safe feedback. Local browser evidence B07–B09 verifies the callback, logout and protected-route cycle. The current candidate has 79 passing tests, passing typecheck/build and clean lint. Hosted CI, deployed provider callback, expiry/error recovery and independent QA remain open.
- 2026-09-24 implementation follow-up: Group DM avatar projections now include the viewer when only two active members remain, keep peers first with explicit overlap layers, preserve one-to-one peer avatars, and announce the full participant count. Added membership-scoped mention identity plus allowlist-based HTML sanitization for sends, edits, scheduled edits, forwarding and all message preview/render paths; user-controlled system-event details are stored as safe plain text. The latest local suite has 105 passing unit/component/integration tests; lint, typecheck and production build pass. A05 and independent feature QA remain open pending full candidate review, legacy-data policy and QA evidence.
- 2026-09-24 W05 performance follow-up: Added a batched published-root channel unread aggregate, SQL keyset paging for the DM inbox before page-only hydration, notification membership filtering before bounded pagination, and a membership-scoped relational notification-read update. Added message activity and notification inbox/unread indexes with migrations. Red/green PostgreSQL tests cover 50 channels, 26 DM conversations and notification page/read boundaries; the local suite is now 119 passing tests. Local instrumentation captured four statements for the 50-channel summary request. Ordinary message/read, pin/unpin and scheduled-message edit/cancel/send-now route invalidation has been removed and read cursor writes are coalesced on focused conversations, but cold/warm latency, production query plans, schedule-creation/membership/admin invalidations and independent QA remain open; no 10× wall-clock result is claimed.
- 2026-09-24 W05 invalidation checkpoint (139-test snapshot): Removed broad route invalidation from channel create/delete/leave, membership/settings changes, one-to-one/group DM create and group rename/leave, scheduled-message creation, profile edits and workspace creation. The owning clients now refresh or navigate targeted views, with component and PostgreSQL regression coverage. The complete local suite is 139 tests; typecheck, lint and production build pass. Targeted settings/invite invalidations, query projections, event amplification, fixed-fixture latency plans and independent QA remain open; no 10× wall-clock result is claimed.
- 2026-09-24 W05 projection/realtime checkpoint (144-test snapshot): Sidebar data now selects only display columns and the current actor's read cursor; DM participant profiles are fetched only for direct channels. Search/forward pickers return only channel IDs/names and require workspace plus channel membership. A component regression failed on the old sidebar-to-window relay and now proves the focused message subscription is the only detail-hydration path. The local candidate has 144 passing tests with typecheck, lint and production build green. At that checkpoint, settings/invite ownership, other list projections, same-subscription duplicate-event dedupe, fixed-fixture cold/warm timings and plans, and independent QA were still open. No 10× wall-clock result is claimed.
- 2026-09-24 OAuth registration inspection: Read the `slack-vibe` GitHub OAuth application settings without editing them. The registration displayed a local development callback but no deployed hostname; the deployed environment's client-ID association remains unknown. B10 therefore supports, but does not by itself prove, the root cause of B02. Resolve the client-ID/environment pairing and obtain action-time confirmation before changing this security-sensitive setting.
- 2026-09-24 logout privacy follow-up: Account-menu logout clears only the current user's `slack-vibe:draft:<userId>:` localStorage entries before invoking Auth.js sign-out. Other users' drafts and unrelated preferences remain untouched, and blocked browser storage cannot prevent logout. New unit/component coverage brings the candidate to 81 passing tests. Browser evidence B07–B09 predates this cleanup and does not verify localStorage deletion; independent QA, encrypted offline drafts and cross-device draft sync remain open.
- 2026-09-19 implementation follow-up: Added per-user composer draft restore/clear persistence scoped by account, workspace conversation and thread. The same DM increment added the pair-key migration, access-checked inbox route, unread/cursor views, bounded recipient/group creation, new-generation expansion, creator-controlled group rename/leave actions, an access-checked members directory and preselected member-to-DM entry, participant avatar stacks and mobile Back navigation. Focused integration/component coverage now totals 48 passing tests; server-synchronized drafts, participant revision/history policy, external delivery, offline outbox and independent QA remain open.
- 2026-09-18 DM documentation follow-up: Added [direct-messages.md](direct-messages.md) as the detailed contract for the implemented one-to-one `DIRECT` slice and the remaining DM inbox, group participant/history policy, notifications, presence, files, calls, meetings, offline outbox, mobile, abuse and governance exploration. Updated the feature, route, system, architecture, implementation, timeline, testing and QA references. F13 now records implementation-slice evidence while independent QA and F14/F52 remain open.
- 2026-09-18 implementation follow-up: Verified local GitHub OAuth with the signed-in GitHub session; the app now reaches an authenticated workspace. Added TDD-backed workspace namespace, channel and message access checks for browse, joins, member reads, stars, reactions, pins, forwarding, scheduled reads and edit/send-now/cancel transitions, direct-message creation with a dedicated DIRECT channel type, participant-aware sidebar/header/composer identity, workspace member listings, workspace-scoped profiles, private search, upload membership/size/type gates, bookmark privacy, notification privacy, optimistic read rollback, duplicate notification suppression and mark-all-read UI. Replaced the forwarding placeholder with an accessible destination-channel dialog backed by server membership and destination-posting checks. Added scheduled-page cancellation and channel-audience filtering, functional invite-link copy, the first production mobile navigation affordance and responsive thread/profile replacement panels, and wired profile direct-message creation to the existing DM action. Typecheck, production build and 35 automated tests pass; full lint still contains pre-existing source debt.
- 2026-09-18 migration follow-up: The existing database had the baseline schema but no Prisma migration history, so deployment initially stopped at P3005. The existing baseline was registered as applied, then the idempotent `DIRECT` channel-type migration was applied successfully. Future schema changes must preserve this migration history instead of relying on ad hoc production SQL.

- 2026-09-18 full-plan follow-up: Added P00–P14 sequence, all-feature/route/package traceability, 642 initial concrete planned cases plus universal/route/integration coverage, and the repair ledger. The current catalogue grows to 649 with later retry cases. Added provisional independent QA allowance 64–122d separately from 161.5–303.5 developer/shared-test days. Browser inspection reached GitHub repository/Actions and both app login flows: deployed OAuth callback rejected, local auth Configuration error. No authenticated feature pass, credential/config change, application implementation or deployment. Earlier unestimated QA entries below describe their historical state.

- 2026-09-18 implementation follow-up: Started P00/P01 under TDD. Added Vitest unit/component/integration projects, isolated PostgreSQL database setup, auth/build contract checks, workspace membership transaction coverage, hook-order regression coverage and server-side channel membership/parent/selected-poster checks for message reads and sends. Typecheck, production build and six integration tests pass; full lint remains blocked by pre-existing source debt outside this slice. OAuth provider configuration still requires environment-specific repair; no production deployment or credential change was made.

- 2026-09-18 QA follow-up: Added a dedicated QA engineer role and QG01–QG08 gates, plus 90 feature and 78 route ledger entries and ten integrated journeys, all NOT RUN. Connected independent sign-off to TDD, feature/package completion and release planning. QA staffing/capacity is separate and not yet estimated; no person hired, agent started or application verification performed in this documentation round.

- 2026-09-18 TDD follow-up: Added mandatory tdd.md and a planned test matrix for all 108 requirements. Revised W01 test foundation from 0.5–1.5d to 2–4d and W18 shared test program from 2–3d to 5–9d, a 4.5–8.5d net increase; per-feature test effort remains within its existing package and needs re-estimation after the first measured slice. Updated coverage/CI/release rules and totals. No application tests, dependencies or workflows were implemented in this documentation round.

- 2026-09-18 mobile follow-up: Made whole-application responsiveness mandatory under F40. Added mobile-responsive.md, fourteen MR acceptance criteria, route-family coverage, focused source evidence and device/keyboard/touch/media/performance contracts. Connected design, architecture, implementation and release gates; no application code or physical-device test performed. Renumbered two colliding proposed-decision IDs to D21/D22 without changing their substance.

- 2026-09-18 calls/platform follow-up: Expanded to 90 user-facing capabilities, 108 requirement IDs, 78 routes and 39 package IDs. Added detailed voice/video, huddle/room, event/calendar, media artifact, community, automation and enterprise contracts. Resolved W20's previously unestimated features into bounded packages; original baseline forecasts remain historical subtotals. Targeted source search found message scheduling but no media/meeting engine; no implementation or provider provisioning occurred.

- 2026-09-18 feature-scope follow-up: Made 60 user-facing capabilities explicit in the feature catalogue, excluding infrastructure and route count. Added F69–F78 for polls, voice notes, custom emoji, groups, templates, acknowledgement requests, tasks, notes, keyword alerts and saved searches. Added detailed collaboration contracts, R53–R58 and W27–W30 with incremental estimates. These are proposed expansion, not audited implementation or automatic additions to day one; original W20 expansions remain unestimated.

- 2026-09-18 follow-up: Added a production route registry, 20 feature requirements and six incremental packages. Expanded screen compositions, data/cache/access/test contracts and forecast. Existing source findings remain the original assessment; no implementation was performed.

- 2026-09-18: Initial repository assessment and linked planning set. Incorporated the user's one-day delivery target and request for precise, high-detail animation throughout the product. Recorded local check failures and logged-out-only visual verification. All future behavior remains proposed.
- 2026-09-24 W05 cache-ownership checkpoint (146 tests): Added red/green PostgreSQL tests showing notification preferences persist without route invalidation and invite membership commits before redirect without refreshing either route tree. At that checkpoint the local total was 146 passing tests; typecheck/lint and the 146-test suite/build passed after action changes. Remaining W05 work is other list projections, mounted reconnect/multi-tab burst evidence, fixed-fixture cold/warm and query-plan measurements, and independent QA.
- 2026-09-24 W05 realtime dedupe follow-up: The focused message listener now reserves a message ID before fetching its detail, caps recent IDs at 500, and releases the reservation if fetch throws. A red/green component regression verifies that identical INSERT deliveries produce one detail request; the sidebar remains responsible only for unread projection. Full cross-tab/reconnect burst evidence is still pending, along with residual projections, fixed-fixture measurements and independent QA. The 146-test total at that checkpoint is unchanged.
- 2026-09-24 W06 root/publication guard: A red/green component case showed that root timeline realtime handling hydrated both thread replies and future scheduled rows. The focused list now accepts only explicitly root, unscheduled and non-deleted inserts; the thread listener remains responsible for reply refresh. The 147-test suite, typecheck, lint and production build pass; full cross-view ordering, live two-tab and reconnect checks remain open.
- 2026-09-24 W06 same-user event reconciliation: Removed the own-user realtime drop after adding a cache-ID check and optimistic/canonical reconciliation. A second tab now hydrates the same user’s message; if the event lands before the action acknowledgement, the acknowledgement removes the optimistic duplicate; if it lands afterward, the cache check avoids a detail fetch. Red/green component and hook regressions cover both orders. Still open: real-browser two-tab/reconnect proof, cross-view updates and server-side idempotency for ambiguous retries.
- 2026-09-24 local verification checkpoint (150 tests): `npm test` passes all 150 tests across 42 files; `npm run lint`, `npm run typecheck` and `npm run build` pass. Live browser inspection of the authenticated `Project Crew` group DM shows the accessible “3 participants” avatar-stack label in the sidebar, inbox and conversation header. Component and route tests cover overlapped rendering and the two-member-after-leave state. This is local candidate evidence; independent QA, hosted CI and live two-tab/reconnect verification remain open.
- 2026-09-24 W04 send-intent idempotency: A red PostgreSQL test reproduced two durable rows when the same client intent was sent twice. Migration `20260924230000_message_send_idempotency` adds a unique author/key index and a stored request fingerprint; matching sequential and concurrent retries return the canonical row, changed channel/content/thread/schedule/attachments conflict, and membership revocation prevents replay. The public action now rejects missing or malformed keys. Prisma omits the fingerprint from normal reads and acknowledgements. The composer hook retains the key and successful file-upload descriptors on its failed optimistic row, so an in-page retry does not upload those files again. The full local suite now passes 158 tests across 42 files, with typecheck, lint and production build green. Other message writers, durable offline retries, explicit uncertain-delivery resolution, scheduled publication, private upload finalization/orphan cleanup and independent QA remain open.
- 2026-09-24 W06 thread/root cache convergence: A red component regression showed that a published reply invalidated only the open thread query, leaving the channel timeline reply-count projection stale. The thread listener now refreshes both active thread rows and the root timeline; scheduled and soft-deleted rows are ignored. Full verification passes with 160 tests across 43 files, lint, typecheck and production build. Live two-tab/reconnect proof, wider update/delete/reaction convergence and independent QA remain open.
- 2026-09-24 W39 group-DM retry/history/avatar boundary: Group creation is idempotent per client intent, not deduplicated by participant set, so a later deliberate group can be separate after the prior intent clears. The client stores the pending UUID/request in the actor/workspace draft namespace, removes it after success and purges it at logout; PostgreSQL persists the unique key and private normalized-request hash. A changed payload or another creator cannot reuse the key. Adding a person creates a separate conversation generation rather than copying history; integration coverage proves the new participant can read the new generation but not the earlier group's messages. Group avatar surfaces retain a second group marker when only one active participant remains without falsifying the active count. Full local verification now passes 173 tests across 45 files, lint, typecheck and production build. In-place participant revisions, broader derived-access coverage, hosted CI and independent QA remain open.
- 2026-09-24 W06 reconnect catch-up: Supabase channel status failures now arm a one-shot recovery; a later `SUBSCRIBED` invalidates the active timeline and refreshes sidebar/notification summaries. Initial subscription does not trigger redundant work, repeated failures coalesce, and unmounted handlers are ignored. Component tests cover the status sequence; an actual network drop/rejoin and two-browser convergence remain open. The full local suite now passes 173 tests across 45 files with lint, typecheck and production build green.
- 2026-09-25 W05 search efficiency: command-search effects ignore result, error and loading completions after a newer query or dialog close. Plain text fetches no member/channel suggestion lists; `from:` and `in:` fetch only their respective lists. Search result hydration selects message ID/channel ID/content/time and author/channel names only; it omits attachments, reactions and unrelated profile/channel fields. Red/green component and PostgreSQL integration tests cover TC-F31-04, close cleanup, suggestion request scope and TC-F31-07's bounded payload. Indexed plain-text search, scoped query cache and latency evidence remain open.
- 2026-09-25 W14 search-filter validation: An invalid `after:` date, unsupported `has:`/`is:` value, empty date, duplicate filter, reversed range, blank query or query over 500 characters could return a wider result set or an unhelpful empty state. Added a pure bounded parser, explicit accessible validation feedback and inclusive UTC calendar-day semantics for date-only bounds. Red/green unit, PostgreSQL integration and component cases now cover TC-F31-03.
- 2026-09-25 W08/W14 bounded context jump: Search and permalink selection now retrieves at most 50 chronological root messages (24 before, target root, 25 after), carries a reply's parent thread ID, centers the target, and lets the user return to the previous timeline. Inaccessible/deleted/future targets remain neutral; a database failure propagates as a retryable generic error instead of looking like a missing message. Red/green integration/component tests cover the bounded projection, thread route, scroll/restore, unavailable state and retry. F23/F31 remain partial: browser history/focus restoration, realistic latency, normalized plain-text indexing, query cache and independent QA are open.
- 2026-09-25 W14 search measurement: Added opt-in `npm run test:performance`, which creates an isolated database, seeds 100,000 messages across 50 authorized channels and 200 users, and reports natural `EXPLAIN (ANALYZE, BUFFERS)` plus warm action samples after five warmups. The initial 12.20ms p50/13.67ms p95 run is historical; five expanded local runs now measure six query shapes, an eight-request burst and natural/forced plans. Selective-query latency and planner choice vary; the natural trigram path was chosen in two of five runs. Keep latency/plan output observational, retain deterministic correctness/index-compatibility tests, and do not add a search-result cache until production-like cold routes, filters and concurrent load are measured. These synthetic local results do not establish production latency or a 10× gain; see [performance evidence](performance.md#w14-fixed-fixture-search-benchmark-25-september-2026).
- 2026-09-25 W39 group-add disclosure: The header's Add people action starts a new group generation to preserve prior history privacy. Its dialog previously looked like ordinary creation and did not disclose the consequence. A red/green component regression now requires a generation-specific title and clear notice that new participants cannot read earlier messages; the generation boundary still has its database access test. Broader file/search/notification/export and independent QA remain open.
- 2026-09-25 W08/W14 history restoration: Keep `message` in the URL while a bounded context is active so same-conversation Back exits the jump and Forward reopens it. The explicit Return to latest action removes only `message`, preserving `thread`. A bounded in-memory map restores scroll and focused message by actor/workspace/conversation across route remounts and stores no message content. Component tests verify this contract; live browser history, mobile behavior and independent QA remain open.
- 2026-09-25 CI action runtime: Hosted run 36124636305 passed application gates but warned that `actions/checkout@v4` and `actions/setup-node@v4` depended on the deprecated Node 20 action runtime. Updated both official actions to current major v7 while retaining Node 22.23.2 for the application build. A red/green release-configuration contract rejects majors v1–v6 so the warning cannot silently return. The next hosted run must verify runner compatibility and warning removal.
